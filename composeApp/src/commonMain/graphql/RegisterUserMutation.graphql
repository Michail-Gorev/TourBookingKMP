mutation RegisterUserMutation($input: UserInput!) {
    createUser(input: $input) {
        token
        user {
            id
            firstName
            lastName
            email
        }
    }
}
